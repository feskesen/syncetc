// USER-PAGE-applicant-portal-current.js
// Internal Version: 2026-06-10-099-A
// Purpose: Applicant-only self-service portal for viewing/updating an application and completing upload tasks.

(function () {
  "use strict";

  const VERSION = "2026-06-10-099-A";
  const SUPABASE_URL = "https://bxywokidhgppmlzyqvem.supabase.co";
  const SUPABASE_PUBLISHABLE_KEY = "sb_publishable_okF_HCqwt-0zcSqlifSZ7g_1kCXxdCA";
  const ACCESS_EDGE_URL = `${SUPABASE_URL}/functions/v1/core-access-action`;
  const SUPABASE_JS_URL = "https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2";
  const ROOT_ID = "syncetc-applicant-portal-root";
  const DEBUG = new URLSearchParams(location.search).has("syncetc_debug") || new URLSearchParams(location.search).has("debug");
  const startMs = Date.now();
  const steps = [];
  const state = { token: "", email: "", payload: null, applicant: null, settings: {}, organization: null, style: {}, loading: true, message: "", error: "", dirty: false, saving: false };

  function mark(label, detail) { if (DEBUG) steps.push(`${String(Date.now() - startMs).padStart(5)}ms  ${label}${detail ? " — " + detail : ""}`); }
  function root() { return document.getElementById(ROOT_ID); }
  function clean(v) { return String(v ?? "").replace(/\s+/g, " ").trim(); }
  function esc(v) { return String(v ?? "").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&#039;"); }
  function attr(v) { return esc(v).replace(/`/g,"&#096;"); }
  function obj(v) { return v && typeof v === "object" && !Array.isArray(v) ? v : {}; }
  function arr(v) { return Array.isArray(v) ? v : []; }
  function byId(id) { return document.getElementById(id); }
  function val(id) { const el = byId(id); return el ? el.value : ""; }
  function rootData() { const r = root(); return { organizationKey: r?.dataset.organizationKey || r?.dataset.customerKey || "test-customer-1" }; }
  function fmtDate(v) { if (!v) return ""; const d = new Date(v); return Number.isNaN(d.getTime()) ? "" : d.toLocaleDateString(); }
  function fmtDateTime(v) { if (!v) return ""; const d = new Date(v); return Number.isNaN(d.getTime()) ? "" : d.toLocaleString(); }

  function styleVars() {
    const colors = obj(state.style.colors_json);
    return `--ap-primary:${esc(colors.brand_primary || "#265c2b")};--ap-soft:${esc(colors.brand_secondary || "#edf7ed")};--ap-text:${esc(colors.text || "#142417")};--ap-surface:${esc(colors.surface || "#fff")};`;
  }

  function css() { return `
.ap-wrap{max-width:1180px;margin:24px auto 48px;padding:0 16px;font-family:Arial,Helvetica,sans-serif;color:var(--ap-text,#142417)}.ap-wrap *{box-sizing:border-box}.ap-panel{background:var(--ap-surface,#fff);border:1px solid color-mix(in srgb,var(--ap-primary,#265c2b) 16%,#d9e2ec);border-radius:24px;box-shadow:0 14px 40px rgba(12,38,64,.14);overflow:hidden}.ap-hero{padding:24px;background:linear-gradient(135deg,var(--ap-primary,#265c2b),color-mix(in srgb,var(--ap-primary,#265c2b) 55%,#4c8f55));color:#fff}.ap-hero h1{margin:0;font-size:clamp(30px,4vw,48px);letter-spacing:-.035em}.ap-hero p{margin:9px 0 0;max-width:820px;color:rgba(255,255,255,.9);font-weight:800;line-height:1.45}.ap-body{padding:16px;background:color-mix(in srgb,var(--ap-soft,#edf7ed) 38%,#fff)}.ap-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.ap-card{background:#fff;border:1px solid color-mix(in srgb,var(--ap-primary,#265c2b) 14%,#d9e2ec);border-radius:18px;padding:14px}.ap-card h2,.ap-card h3{margin:0 0 8px;color:var(--ap-primary,#265c2b)}.ap-label{font-size:11px;text-transform:uppercase;letter-spacing:.05em;font-weight:950;color:var(--ap-primary,#265c2b)}.ap-field{display:grid;gap:5px;margin-top:10px}.ap-input,.ap-select,.ap-textarea{width:100%;padding:10px 11px;border:1px solid #c5d4e2;border-radius:10px;font:inherit;background:#fff;color:#111827}.ap-textarea{min-height:90px;resize:vertical}.ap-pill{display:inline-flex;align-items:center;border-radius:999px;padding:4px 9px;background:color-mix(in srgb,var(--ap-soft,#edf7ed) 82%,#fff);color:var(--ap-primary,#265c2b);font-weight:950;font-size:12px}.ap-alert{padding:10px 12px;border-radius:12px;font-weight:850;margin-bottom:10px}.ap-alert.ok{background:#e7f6ec;color:#17633a}.ap-alert.bad{background:#fee2e2;color:#991b1b}.ap-alert.info{background:#eaf5ff;color:#1f4f82}.ap-btn{border:1px solid var(--ap-primary,#265c2b);background:var(--ap-primary,#265c2b);color:#fff;border-radius:999px;padding:9px 13px;font-weight:900;cursor:pointer;transition:.12s;box-shadow:0 6px 14px rgba(12,38,64,.12)}.ap-btn:hover:not(:disabled){transform:translateY(-1px);box-shadow:0 9px 18px rgba(12,38,64,.18)}.ap-btn.secondary{background:#fff;color:var(--ap-primary,#265c2b)}.ap-btn:disabled{opacity:.55;cursor:not-allowed}.ap-actions{display:flex;gap:8px;flex-wrap:wrap;align-items:center;margin-top:12px}.ap-task{border:1px solid #e1e8f0;border-radius:14px;padding:11px;margin-top:10px;background:#fff}.ap-task-head{display:flex;justify-content:space-between;gap:8px;align-items:flex-start}.ap-task-title{font-weight:950;color:#172033}.ap-task-desc{font-size:13px;color:#52606d;margin-top:3px}.ap-upload{margin-top:10px;padding:10px;border:1px dashed #bfd0df;border-radius:12px;background:#fbfdff}.ap-upload-row{display:grid;grid-template-columns:1fr auto;gap:8px;align-items:center}.ap-upload-list{margin-top:8px;display:grid;gap:6px}.ap-upload-item{font-size:13px;padding:7px 9px;border-radius:10px;background:#f8fafc;border:1px solid #e5edf5}.ap-debug{margin-top:12px;padding:12px;border-radius:12px;background:#101828;color:#dbeafe;font:12px ui-monospace,Menlo,Consolas,monospace;white-space:pre-wrap;overflow:auto}.ap-muted{color:#52606d;font-size:13px;line-height:1.45}@media(max-width:800px){.ap-grid{grid-template-columns:1fr}.ap-upload-row{grid-template-columns:1fr}}`; }

  function loadScript(src) { return new Promise((resolve, reject) => { if ([...document.scripts].some((s) => s.src === src)) return resolve(); const sc = document.createElement("script"); sc.src = src; sc.async = true; sc.onload = resolve; sc.onerror = () => reject(new Error(`Unable to load ${src}`)); document.head.appendChild(sc); }); }
  function waitFor(fn, timeout=8000) { const st=Date.now(); return new Promise((resolve,reject)=>{ (function tick(){ if(fn()) return resolve(); if(Date.now()-st>timeout) return reject(new Error("Timed out waiting for Supabase")); setTimeout(tick,50); })(); }); }
  async function ensureSupabase() { if (!window.supabase?.createClient) await loadScript(SUPABASE_JS_URL); if (!window.supabase?.createClient) await waitFor(()=>window.supabase?.createClient); if (!window.__syncetcApplicantPortalSupabase) window.__syncetcApplicantPortalSupabase = window.supabase.createClient(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY); return window.__syncetcApplicantPortalSupabase; }
  async function accessCall(body) { const client = await ensureSupabase(); const { data } = await client.auth.getSession(); const token = data?.session?.access_token; if (!token) throw new Error("Log in first."); state.token = token; state.email = data.session.user?.email || ""; const res = await fetch(ACCESS_EDGE_URL, { method:"POST", headers:{"Content-Type":"application/json", Authorization:`Bearer ${token}`}, body: JSON.stringify(body) }); const json = await res.json().catch(()=>({})); if(!res.ok || json.ok===false) throw new Error(clean(json.message || json.error || `HTTP ${res.status}`)); return json; }

  async function refresh() { mark("refresh:start"); state.loading = true; state.error = ""; render(); try { const data = await accessCall({ action:"applicant_get_portal", organization_key: rootData().organizationKey }); state.payload = data; state.applicant = data.application || data.applicant || null; state.settings = obj(data.settings); state.organization = data.organization || null; state.style = obj(data.style_profile); state.loading = false; mark("refresh:done", clean(state.applicant?.display_name)); render(); } catch (error) { state.loading=false; state.error = error instanceof Error ? error.message : String(error); render(); } }

  function statusHtml(app) { return `<span class="ap-pill">${esc(app.status_label || app.status || "Application")}</span>${app.waitlist_order ? ` <span class="ap-pill">Waitlist #${esc(app.waitlist_order)}</span>` : ""}${app.ready_for_final_review ? ` <span class="ap-pill">Ready for final review</span>` : ""}`; }
  function applicationForm(app) { const ad=obj(app.address_json), bg=obj(app.background_json), av=obj(app.aviation_json), sf=obj(app.safety_json), interest=obj(app.interest_json); return `<div class="ap-card"><h2>Application information</h2><p class="ap-muted">Update contact, aviation, and interest details while portal updates are enabled.</p><div class="ap-grid"><label class="ap-field"><span class="ap-label">First name</span><input class="ap-input" id="ap-first" value="${attr(app.first_name)}"></label><label class="ap-field"><span class="ap-label">Last name</span><input class="ap-input" id="ap-last" value="${attr(app.last_name)}"></label><label class="ap-field"><span class="ap-label">Phone</span><input class="ap-input" id="ap-phone" value="${attr(app.phone)}"></label><label class="ap-field"><span class="ap-label">Email</span><input class="ap-input" value="${attr(app.email)}" disabled></label><label class="ap-field"><span class="ap-label">Street address</span><input class="ap-input" id="ap-street" value="${attr(ad.street || ad.address)}"></label><label class="ap-field"><span class="ap-label">City</span><input class="ap-input" id="ap-city" value="${attr(ad.city)}"></label><label class="ap-field"><span class="ap-label">State</span><input class="ap-input" id="ap-state" value="${attr(ad.state)}"></label><label class="ap-field"><span class="ap-label">ZIP</span><input class="ap-input" id="ap-zip" value="${attr(ad.zip)}"></label><label class="ap-field"><span class="ap-label">Employer</span><input class="ap-input" id="ap-employer" value="${attr(bg.employer)}"></label><label class="ap-field"><span class="ap-label">Occupation</span><input class="ap-input" id="ap-occupation" value="${attr(bg.occupation)}"></label></div><div class="ap-grid"><label class="ap-field"><span class="ap-label">Ratings / endorsements</span><input class="ap-input" id="ap-ratings" value="${attr(av.ratings)}"></label><label class="ap-field"><span class="ap-label">Medical / BasicMed status</span><input class="ap-input" id="ap-medical" value="${attr(av.medical_status)}"></label><label class="ap-field"><span class="ap-label">Total hours</span><input class="ap-input" id="ap-total" value="${attr(av.total_hours)}"></label><label class="ap-field"><span class="ap-label">Aircraft experience</span><input class="ap-input" id="ap-aircraft" value="${attr(av.aircraft_types)}"></label></div><label class="ap-field"><span class="ap-label">Why do you want to join?</span><textarea class="ap-textarea" id="ap-why">${esc(interest.why_join)}</textarea></label><label class="ap-field"><span class="ap-label">Anything else?</span><textarea class="ap-textarea" id="ap-notes">${esc(interest.additional_notes || obj(app.custom_answers_json).additional_notes)}</textarea></label><div class="ap-actions"><button class="ap-btn" id="ap-save">Save updates</button><span class="ap-muted" id="ap-save-note"></span></div></div>`; }

  function taskHtml(task) { const uploads = arr(task.uploads); const isUpload = ["upload-profile-photo","upload-pilot-certificate","upload-medical-proof"].includes(clean(task.task_key)) || clean(task.task_type)==="upload"; const uploadList = uploads.length ? `<div class="ap-upload-list">${uploads.map(u=>`<div class="ap-upload-item"><strong>${esc(u.original_file_name || 'Uploaded file')}</strong> • ${esc(u.upload_status || 'submitted')}${u.review_note?`<br>Review note: ${esc(u.review_note)}`:""}</div>`).join("")}</div>` : `<div class="ap-muted">No upload submitted yet.</div>`; return `<div class="ap-task"><div class="ap-task-head"><div><div class="ap-task-title">${esc(task.label)}</div><div class="ap-task-desc">${esc(task.description)}</div></div><span class="ap-pill">${esc(task.status || 'pending')}</span></div>${task.note?`<div class="ap-alert info" style="margin-top:8px">${esc(task.note)}</div>`:""}${isUpload?`<div class="ap-upload"><div class="ap-upload-row"><input type="file" id="file-${esc(task.applicant_task_id)}" accept=".pdf,image/*"><button class="ap-btn secondary ap-upload-btn" data-task-id="${esc(task.applicant_task_id)}">Upload</button></div><label class="ap-field"><span class="ap-label">Optional note</span><input class="ap-input" id="note-${esc(task.applicant_task_id)}" placeholder="Optional note about this upload"></label>${uploadList}</div>`:""}</div>`; }

  function html() { const app = state.applicant; const org = state.organization || {}; if (state.loading) return `<style>${css()}</style><div class="ap-wrap" style="${styleVars()}"><div class="ap-panel"><div class="ap-body">Loading applicant portal…</div></div></div>`; if (state.error) return `<style>${css()}</style><div class="ap-wrap"><div class="ap-alert bad">${esc(state.error)} ${state.error.includes('Log in') ? '<a href="/login">Log in</a>' : ''}</div></div>`; if (!app) return `<style>${css()}</style><div class="ap-wrap"><div class="ap-alert bad">No applicant application found.</div></div>`; return `<style>${css()}</style><div class="ap-wrap" style="${styleVars()}"><section class="ap-panel"><div class="ap-hero"><div style="font-size:11px;font-weight:950;letter-spacing:.1em;text-transform:uppercase;opacity:.88">Applicant Portal</div><h1>${esc(org.display_name || 'Applicant Portal')}</h1><p>View your application status and complete requested tasks. This login is applicant-only and does not provide member access.</p></div><div class="ap-body">${state.message?`<div class="ap-alert ok">${esc(state.message)}</div>`:""}<div class="ap-card"><h2>${esc(app.display_name)}</h2><div>${statusHtml(app)}</div><p class="ap-muted" style="margin-bottom:0">Submitted ${esc(fmtDate(app.submitted_at || app.created_at))}${app.last_applicant_update_at?` • Last updated ${esc(fmtDateTime(app.last_applicant_update_at))}`:""}</p></div><div class="ap-grid" style="margin-top:14px">${applicationForm(app)}<div class="ap-card"><h2>Applicant tasks</h2><p class="ap-muted">Tasks may be completed by you, an officer/admin, or the organization depending on the task.</p>${(arr(app.current_stage_tasks).length ? arr(app.current_stage_tasks) : arr(app.tasks)).map(taskHtml).join("") || '<div class="ap-muted">No tasks are currently assigned.</div>'}</div></div>${DEBUG?`<pre class="ap-debug">SyncEtc Applicant Portal ${VERSION}\nElapsed: ${Date.now()-startMs}ms\nEmail: ${esc(state.email)}\nSteps:\n${esc(steps.join("\n"))}\n\n${esc(JSON.stringify(state.payload,null,2))}</pre>`:""}</div></section></div>`; }
  function render() { const r=root(); if(r) r.innerHTML = html(); bind(); }
  function collectUpdate() { return { first_name: val('ap-first'), last_name: val('ap-last'), phone: val('ap-phone'), address_1: val('ap-street'), city: val('ap-city'), state: val('ap-state'), zip: val('ap-zip'), employer: val('ap-employer'), occupation: val('ap-occupation'), ratings: val('ap-ratings'), medical_class: val('ap-medical'), total_hours: val('ap-total'), aircraft_experience: val('ap-aircraft'), why_join: val('ap-why'), anything_else: val('ap-notes') }; }
  async function saveUpdate() { if (state.saving) return; state.saving = true; const note=byId('ap-save-note'); if(note) note.textContent='Saving…'; try { const data = await accessCall({ action:'applicant_save_my_application', organization_key:rootData().organizationKey, ...collectUpdate() }); state.payload=data; state.applicant=data.applicant; state.settings=obj(data.settings); state.message='Application updates saved.'; state.dirty=false; render(); } catch(error){ if(note) note.textContent=''; alert(error.message || error); } finally { state.saving=false; } }
  function readFileAsDataUrl(file) { return new Promise((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result || "")); reader.onerror = () => reject(new Error("Could not read selected file.")); reader.readAsDataURL(file); }); }
  async function uploadTask(taskId) { const fileEl=byId(`file-${taskId}`); const file=fileEl?.files?.[0]; if(!file) return alert('Choose a file first.'); try { const dataUrl = await readFileAsDataUrl(file); const data = await accessCall({ action:'applicant_upload_task_file', organization_key:rootData().organizationKey, applicant_task_id:taskId, file_name:file.name, mime_type:file.type, file_size_bytes:file.size, data_url:dataUrl, applicant_note:val(`note-${taskId}`) }); state.payload=data; state.applicant=data.application || data.applicant; state.message='Upload submitted for review.'; render(); } catch(error){ alert(error.message || error); } }
  function bind() { const save=byId('ap-save'); if(save) save.onclick=saveUpdate; document.querySelectorAll('.ap-upload-btn').forEach(btn=>btn.addEventListener('click',()=>uploadTask(btn.dataset.taskId))); document.querySelectorAll('.ap-input,.ap-textarea').forEach(el=>el.addEventListener('input',()=>{state.dirty=true;})); }
  function bindNavAway() { window.addEventListener('beforeunload',(event)=>{ if(!state.dirty) return; event.preventDefault(); event.returnValue=''; }); }
  async function init(){ const r=root(); if(!r) return; r.innerHTML='Loading applicant portal…'; bindNavAway(); await refresh(); }
  if(document.readyState==='loading') document.addEventListener('DOMContentLoaded',init); else init();
})();
