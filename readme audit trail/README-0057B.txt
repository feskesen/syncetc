README-0057B — Portal Page Activation SQL Constraint Fix

Purpose
- Corrects the 0057 SQL migration so core_template_registry.complexity_level uses the existing allowed values.

Change
- Roster complexity: moderate -> medium
- People & Access complexity: advanced -> complex

Install
- Use PORTAL-PAGE-ACTIVATION-0057B.sql instead of the 0057 SQL file.

No structural change from 0057.
