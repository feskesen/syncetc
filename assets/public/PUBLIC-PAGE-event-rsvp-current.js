// PUBLIC-PAGE-event-rsvp-current.js
// Internal Version: 2026-06-09-088-A
// Purpose: Event RSVP renderer with logged-in People-linked RSVP path and public RSVP fallback.

(function () {
  "use strict";
  const VERSION = "2026-06-09-088-A";
  const SUPABASE_URL = "https://bxywokidhgppmlzyqvem.supabase.co";
  const SUPABASE_ANON_KEY = "sb_publishable_okF_HCqwt-0zcSqlifSZ7g_1kCXxdCA";
  const PUBLIC_EDGE_URL = `${SUPABASE_URL}/functions/v1/core-public-render`;
  const ACCESS_EDGE_URL = `${SUPABASE_URL}/functions/v1/core-access-action`;
  const SUPABASE_JS = "https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2";
  const ROOT_SELECTOR = "#syncetc-event-rsvp-root";
  const DEBUG = new URLSearchParams(location.search).has("syncetc_debug") || new URLSearchParams(location.search).has("debug");
  let supabaseClient = null;
  let token = "";
  let state = { mode:"public", payload:null, accessRow:null, organizationId:"", myRsvp:null, rsvps:[], saving:false };

  function e(v){return String(v??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&#039;");}
  function clean(v){return String(v??"").replace(/\s+/g," ").trim();}
  function qs(name){try{return new URLSearchParams(location.search).get(name)||"";}catch{return "";}}
  function val(id){const x=document.getElementById(id);return x?x.value:"";}
  function checked(id){const x=document.getElementById(id);return !!(x&&x.checked);}
  function fmt(v){if(!v)return"";try{return new Date(v).toLocaleString();}catch{return"";}}
  function number(v){const n=Number(v);return Number.isFinite(n)?n:0;}
  function eventId(){return qs('event')||qs('event_id')||qs('id');}
  function orgKey(root){return root.dataset.organizationKey||root.dataset.customerKey||'test-customer-1';}
  function safeStatus(v){return ['yes','maybe','no','waitlist','no_response','cancelled'].includes(v)?v:'yes';}

  function css(){return `.rsvp-page{max-width:980px;margin:0 auto;font-family:Arial,Helvetica,sans-serif;color:#172033}.rsvp-page *{box-sizing:border-box}.rsvp-shell{background:#fff;border:1px solid #d9e2ec;border-radius:24px;box-shadow:0 14px 42px rgba(12,38,64,.14);overflow:hidden}.rsvp-hero{padding:28px;background:linear-gradient(135deg,#1f4f82,#5d99cf);color:#fff}.rsvp-hero h1{margin:0;font-size:clamp(30px,4vw,46px)}.rsvp-hero p{margin:8px 0 0;color:rgba(255,255,255,.9);font-weight:800}.rsvp-main{padding:22px;background:#f7fbff}.rsvp-card{background:#fff;border:1px solid #d9e2ec;border-radius:18px;padding:18px;margin-bottom:16px}.rsvp-event{display:grid;grid-template-columns:90px minmax(0,1fr);gap:16px;align-items:center}.rsvp-date{background:#1f4f82;color:#fff;border-radius:14px;text-align:center;padding:12px}.rsvp-date strong{display:block;font-size:28px}.rsvp-meta{color:#667;font-weight:800;font-size:13px}.rsvp-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.rsvp-field{display:grid;gap:6px;margin-bottom:12px}.rsvp-field span,.rsvp-label{font-size:12px;font-weight:900;text-transform:uppercase;color:#294968}.rsvp-field input,.rsvp-field textarea,.rsvp-field select{width:100%;padding:11px 12px;border:1px solid #bfd0e0;border-radius:10px;font:inherit}.rsvp-choices{display:flex;gap:10px;flex-wrap:wrap}.rsvp-choice{padding:12px 18px;border:1px solid #bfd0e0;border-radius:14px;cursor:pointer;background:#fff;font-weight:900}.rsvp-choice input{margin-right:6px}.rsvp-choice:has(input:checked){border-color:#1f4f82;background:#eaf5ff;color:#1f4f82}.rsvp-check{display:flex;align-items:center;gap:8px;padding:11px 12px;border:1px solid #d9e2ec;border-radius:12px;background:#fff;font-weight:800}.rsvp-btn{border:1px solid #1f4f82;border-radius:999px;padding:10px 16px;background:#1f4f82;color:#fff;font-weight:900;cursor:pointer}.rsvp-btn.secondary{background:#fff;color:#1f4f82}.rsvp-alert{padding:12px 14px;border-radius:12px;margin-bottom:12px;font-weight:800}.rsvp-alert.ok{background:#e7f6ec;color:#17633a}.rsvp-alert.bad{background:#fee2e2;color:#991b1b}.rsvp-alert.warn{background:#fff4d8;color:#8a5700}.rsvp-disabled{padding:18px;border:1px dashed #bfd0e0;border-radius:14px;text-align:center;color:#667;background:#fff}.rsvp-summary-grid{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:8px}.rsvp-stat{padding:12px;border:1px solid #bfd0e0;border-radius:12px;background:#eaf5ff}.rsvp-stat small{display:block;text-transform:uppercase;font-size:10px;font-weight:900;color:#294968}.rsvp-stat strong{font-size:18px}.rsvp-table{width:100%;border-collapse:collapse;font-size:13px}.rsvp-table th,.rsvp-table td{border-bottom:1px solid #e5edf5;padding:8px;text-align:left}.rsvp-pill{display:inline-flex;border-radius:999px;padding:5px 9px;background:#eaf5ff;color:#1f4f82;font-weight:900;font-size:11px}.rsvp-debug{padding:12px;border-radius:12px;background:#101828;color:#dbeafe;font:12px ui-monospace,Menlo,Consolas,monospace;white-space:pre-wrap}@media(max-width:720px){.rsvp-grid,.rsvp-event{grid-template-columns:1fr}.rsvp-summary-grid{grid-template-columns:1fr 1fr}}`}
  function month(v){try{return new Date(v).toLocaleDateString(undefined,{month:'short'});}catch{return'';}}
  function day(v){try{return String(new Date(v).getDate());}catch{return'';}}
  function eventHeader(ev){return `<section class="rsvp-card rsvp-event"><div class="rsvp-date"><div>${e(month(ev.starts_at))}</div><strong>${e(day(ev.starts_at))}</strong><div>${new Date(ev.starts_at||Date.now()).getFullYear()}</div></div><div><span class="rsvp-pill">${e(ev.category||'Event')}</span><h2>${e(ev.title)}</h2><div class="rsvp-meta">${e(fmt(ev.starts_at))}${ev.location_name?' • '+e(ev.location_name):''}</div>${ev.location_address?`<div class="rsvp-meta">${e(ev.location_address)}</div>`:''}</div></section>`;}
  function statusChoiceHtml(current){return `<div class="rsvp-field"><span>Are you coming?</span><div class="rsvp-choices"><label class="rsvp-choice"><input type="radio" name="rsvp-status" value="yes" ${current==='yes'?'checked':''}>Yes</label><label class="rsvp-choice"><input type="radio" name="rsvp-status" value="maybe" ${current==='maybe'?'checked':''}>Maybe</label><label class="rsvp-choice"><input type="radio" name="rsvp-status" value="no" ${current==='no'?'checked':''}>No</label></div></div>`;}
  function formHtml(ev){const r=state.myRsvp||{};const current=safeStatus(r.response_status||'yes');const isMember=state.mode==='member';const can=ev.viewer_can_rsvp||ev.public_can_rsvp||ev.can_rsvp; if(!can) return `<div class="rsvp-disabled">${e(ev.viewer_rsvp_reason||'RSVP is not available for your account or this event.')}</div>`; const maxGuests=Number(ev.max_guests_per_rsvp||0); return `<section class="rsvp-card"><div id="rsvp-alert"></div><div class="rsvp-label">Your RSVP</div>${statusChoiceHtml(current)}${!isMember?`<div class="rsvp-grid"><label class="rsvp-field"><span>Name *</span><input id="rsvp-name" value="${e(r.respondent_name||'')}"></label><label class="rsvp-field"><span>Email</span><input id="rsvp-email" type="email" value="${e(r.respondent_email||'')}"></label></div>`:`<div class="rsvp-alert ok">This RSVP will be linked to your member/person record.</div>`}<label class="rsvp-check"><input id="rsvp-personal" type="checkbox" ${r.is_attending_personally!==false?'checked':''}> I am attending personally</label><div class="rsvp-grid"><label class="rsvp-field"><span>Additional adults</span><input id="rsvp-adults" type="number" min="0" max="${e(maxGuests||99)}" value="${e(r.additional_adults??r.adult_count??0)}"></label><label class="rsvp-field"><span>Additional children</span><input id="rsvp-children" type="number" min="0" max="${e(maxGuests||99)}" value="${e(r.additional_children??r.child_count??0)}"></label></div><label class="rsvp-field"><span>Shared RSVP note</span><textarea id="rsvp-note" placeholder="Optional note visible with your RSVP.">${e(r.shared_note||'')}</textarea></label><label class="rsvp-field"><span>Private note to self</span><textarea id="rsvp-private-note" placeholder="Optional note saved with your RSVP but not shown in attendee list.">${e(r.private_note||'')}</textarea></label><div style="display:flex;justify-content:space-between;gap:12px;align-items:center;flex-wrap:wrap"><strong>Total for your RSVP: <span id="rsvp-total">1</span></strong><button id="rsvp-submit" class="rsvp-btn">Save Changes</button></div></section>`;}
  function summaryHtml(){ const ev=state.payload?.event||{}; const c=state.payload?.summary || state.payload?.rsvp_counts || ev.rsvp_counts || {}; const rows=state.rsvps||[]; return `<section class="rsvp-card"><div class="rsvp-label">Who's coming</div><h3>RSVP Summary</h3><div class="rsvp-summary-grid"><div class="rsvp-stat"><small>Yes</small><strong>${e(c.yes||0)}</strong></div><div class="rsvp-stat"><small>Maybe</small><strong>${e(c.maybe||0)}</strong></div><div class="rsvp-stat"><small>No</small><strong>${e(c.no||0)}</strong></div><div class="rsvp-stat"><small>Waitlist</small><strong>${e(c.waitlist||0)}</strong></div><div class="rsvp-stat"><small>Total</small><strong>${e(c.total_attendees||0)}</strong></div></div>${rows.length?`<table class="rsvp-table"><thead><tr><th>Member/contact</th><th>Status</th><th>Total</th><th>Note</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${e(r.respondent_name||'')}</td><td><span class="rsvp-pill">${e(r.response_status||'')}</span></td><td>${e(r.attendee_count||0)}</td><td>${e(r.shared_note||'')}</td></tr>`).join('')}</tbody></table>`:'<div class="rsvp-disabled" style="margin-top:12px">No attendee list is available for this view.</div>'}</section>`;}
  function render(){ const root=document.querySelector(ROOT_SELECTOR); if(!root) return; const ev=state.payload?.event; if(!ev){ root.innerHTML='<div style="padding:20px">Loading RSVP…</div>'; return; } root.innerHTML=`<style>${css()}</style><section class="rsvp-page"><div class="rsvp-shell"><header class="rsvp-hero"><div class="rsvp-pill" style="background:rgba(255,255,255,.18);color:#fff">Club Event RSVP</div><h1>Submit or update your RSVP</h1><p>Confirm the event below, update your RSVP, and see who else is coming.</p></header><main class="rsvp-main"><div style="text-align:right;margin-bottom:10px"><a class="rsvp-btn secondary" href="/calendar">Return to Calendar</a></div>${eventHeader(ev)}${formHtml(ev)}${summaryHtml()}${DEBUG?`<pre class="rsvp-debug">RSVP ${VERSION}\nMode: ${e(state.mode)}\nEvent: ${e(ev.event_id)}\nCan RSVP: ${e(ev.viewer_can_rsvp||ev.public_can_rsvp||ev.can_rsvp)}</pre>`:''}</main></div></section>`; bind(); updateTotal(); }
  function updateTotal(){ const status=document.querySelector('input[name="rsvp-status"]:checked')?.value||'yes'; const total=status==='yes'?Math.max(0,(checked('rsvp-personal')?1:0)+number(val('rsvp-adults'))+number(val('rsvp-children'))):0; const el=document.getElementById('rsvp-total'); if(el) el.textContent=String(total); }
  function alert(msg,kind='ok'){ const el=document.getElementById('rsvp-alert'); if(el) el.innerHTML=`<div class="rsvp-alert ${kind}">${e(msg)}</div>`; }
  function bind(){ ['rsvp-personal','rsvp-adults','rsvp-children'].forEach(id=>document.getElementById(id)?.addEventListener('input',updateTotal)); document.querySelectorAll('input[name="rsvp-status"]').forEach(x=>x.addEventListener('change',updateTotal)); const btn=document.getElementById('rsvp-submit'); if(btn) btn.onclick=saveRsvp; }
  async function ensureSupabase(){ if(supabaseClient) return supabaseClient; if(!window.supabase||!window.supabase.createClient){ await new Promise((resolve,reject)=>{ const existing=[...document.scripts].find(s=>String(s.src||'').includes('supabase-js')); if(existing&&window.supabase?.createClient) return resolve(); if(existing){ existing.addEventListener('load',()=>resolve(),{once:true}); existing.addEventListener('error',()=>reject(new Error('Supabase script failed.')),{once:true}); return; } const sc=document.createElement('script'); sc.src=SUPABASE_JS; sc.onload=()=>resolve(); sc.onerror=()=>reject(new Error('Supabase script failed.')); document.head.appendChild(sc); }); } supabaseClient=window.supabase.createClient(SUPABASE_URL,SUPABASE_ANON_KEY); return supabaseClient; }
  async function publicCall(action,body){ const res=await fetch(PUBLIC_EDGE_URL,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,...body})}); const json=await res.json(); if(!res.ok||json.ok===false) throw new Error(json.message||json.error||'Action failed'); return json; }
  async function accessCall(action,body){ const res=await fetch(ACCESS_EDGE_URL,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({action,...body})}); const json=await res.json(); if(!res.ok||json.ok===false) throw new Error(json.message||json.error||'Action failed'); return json; }
  async function tryMemberContext(root){ try{ const sb=await ensureSupabase(); const {data}=await sb.auth.getSession(); token=data?.session?.access_token||''; if(!token) return false; const dashboard=await accessCall('get_user_dashboard',{}); const orgKeyWanted=orgKey(root); const rows=dashboard.access||[]; const row=rows.find(r=>String(r.organization_key||'')===String(orgKeyWanted)) || rows[0]; if(!row?.organization_id) return false; state.organizationId=row.organization_id; state.accessRow=row; const ctx=await accessCall('member_get_event_rsvp_context',{organization_id:row.organization_id,event_id:eventId()}); state.mode='member'; state.payload=ctx; state.myRsvp=ctx.my_rsvp || ctx.existing_rsvp; state.rsvps=ctx.rsvps||[]; return true; }catch(err){ if(DEBUG) console.warn('member rsvp context failed',err); return false; } }
  async function load(root){ root.innerHTML='<div style="padding:20px">Loading RSVP…</div>'; if(await tryMemberContext(root)){ render(); return; } state.payload=await publicCall('get_event_rsvp_page',{event_id:eventId(),organization_key:orgKey(root)}); state.rsvps=[]; state.mode='public'; render(); }
  async function saveRsvp(){ if(state.saving) return; try{ state.saving=true; const status=document.querySelector('input[name="rsvp-status"]:checked')?.value||'yes'; const body={event_id:state.payload.event.event_id,response_status:status,is_attending_personally:checked('rsvp-personal'),additional_adults:number(val('rsvp-adults')),additional_children:number(val('rsvp-children')),adult_count:number(val('rsvp-adults')),child_count:number(val('rsvp-children')),attending_self:checked('rsvp-personal'),shared_note:val('rsvp-note'),private_note:val('rsvp-private-note')}; let res; if(state.mode==='member'){ res=await accessCall('member_save_event_rsvp',{organization_id:state.organizationId,...body}); state.payload=res; state.myRsvp=res.my_rsvp || res.existing_rsvp || res.rsvp; state.rsvps=res.rsvps||[]; } else { const name=clean(val('rsvp-name')); if(!name) throw new Error('Name is required.'); res=await publicCall('submit_event_rsvp',{...body,respondent_name:name,respondent_email:val('rsvp-email')}); state.payload=await publicCall('get_event_rsvp_page',{event_id:eventId()}); } render(); alert(res.message||'RSVP saved.','ok'); }catch(err){ alert(err.message,'bad'); }finally{ state.saving=false; } }
  function init(){ const root=document.querySelector(ROOT_SELECTOR); if(!root) return; load(root).catch(err=>{ root.innerHTML='<div style="padding:20px;border:1px solid #f0b4b4;background:#fee2e2;color:#991b1b;border-radius:12px">'+e(err.message)+'</div>'; }); }
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
